package com.company;

import java.util.ArrayList;

public class CollisionHandler {
    private ArrayList<Collidable> entitiesToRemove;
    private ArrayList<Snake> snakesToGrow;

    /*
    private ArrayList<Collidable> getActorEntities(Board board) {
        ArrayList<Collidable> actorEntities = new ArrayList<>();
        if (board.getSnake() != null) {
            actorEntities.add(board.getSnake());
        }
        if (board.getEnemySnake() != null) {
            actorEntities.add(board.getEnemySnake());
        }
        if (board.getFrogs() != null) {
            actorEntities.addAll(board.getFrogs());
        }
        return actorEntities;
    }

    private ArrayList<Collidable> getTargetEntities(Board board) {
        ArrayList<Collidable> targetEntities = new ArrayList<>();
        if (board.getSnake() != null) {
            targetEntities.add(board.getSnake());
        }
        if (board.getEnemySnake() != null) {
            targetEntities.add(board.getEnemySnake());
        }
        if (board.getFruits() != null) {
            targetEntities.addAll(board.getFruits());
        }
        if (board.getFrogs() != null) {
            targetEntities.addAll(board.getFrogs());
        }
        if (board.getObstacles() != null) {
            targetEntities.addAll(board.getObstacles());
        }
        return targetEntities;
    }
     */
    /*
    private void findCollisions(Board board) {
        if (board.getSnake() != null) {
            foreach()
            if (board.getSnake().getSnakeHead().equals())
        }
    }

    public void flushFoundCollisions(Board board) {

    }

     */
}
